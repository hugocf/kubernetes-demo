apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  creationTimestamp: null
  generation: 1
  labels:
    run: back-app
  name: back-app
  selfLink: /apis/extensions/v1beta1/namespaces/default/deployments/back-app
spec:
  replicas: 1
  selector:
    matchLabels:
      run: back-app
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        run: back-app
    spec:
      containers:
      - image: hugocf/back-app:0.1
        imagePullPolicy: IfNotPresent
        name: back-app
        ports:
        - containerPort: 8000
          protocol: TCP
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
status: {}
